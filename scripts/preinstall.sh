#!/bin/bash
# Скрипт перед установкой/обновлением
# Запускается как root
#
# НЕ удаляем файлы! RPM сам перезапишет их.
# Удаление до установки может сломать систему если
# новые файлы не установятся (кэш, ошибки и т.д.)

exit 0
